//! Anthropic ↔ OpenAI body transforms.
//!
//! Phase 1 ships the module skeleton only — handlers always pick a provider
//! whose kind matches the wire shape (no transform needed). Real conversions
//! land in Phase 2 once we have integration tests.
